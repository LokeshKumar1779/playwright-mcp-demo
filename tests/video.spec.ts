/* import { expect, test } from "@playwright/test";

test("video record test", async ({ page }) => {
  await page.goto("https://www.amazon.com");
  await expect(page).toHaveTitle("/Amazon/");
});
 */
